# graphql docker image build && docker-compose run

## How rto build

* build website(host)

```bash
yarn  && yarn build
```

* build image

```bash
docker-compose build
```

* run

```bash
docker-compose up -d
```